// Kept dependency-free: the test setup imports it before react-native is mocked.
/** Marks FitText's hidden measuring copy so tests can leave it out of text queries. */
export const FIT_TEXT_MEASURE_TEST_ID = "fit-text-measure";
