// The version itself lives in `@dubgrid/domain` so the mobile app can gate on
// the exact same constant. Re-exported here to keep the existing web import path.
export { CURRENT_TERMS_VERSION } from "@dubgrid/domain";
